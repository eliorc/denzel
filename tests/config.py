from denzel_cli.scripts import cli

PROJECT_COMMANDS = [cli.launch, cli.logs, cli.logworker, cli.pinstall, cli.restart, cli.shell, cli.shutdown,
                    cli.start, cli.status, cli.stop, cli.updatereqs]

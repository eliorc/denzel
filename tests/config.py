from denzel_cli.scripts import cli

PROJECT_COMMANDS = [cli.launch, cli.logs, cli.logworker, cli.restart, cli.shell, cli.shutdown,
                    cli.start, cli.status, cli.stop, cli.updatereqs]
